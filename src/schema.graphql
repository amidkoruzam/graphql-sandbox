type Book {
    title: String!
    author: Author!
}

type Author {
    id: Int!
    name: String!
    country: Country!
}

type Country {
    id: Int!
    name: String!
}

type Query {
    books: [Book]
}